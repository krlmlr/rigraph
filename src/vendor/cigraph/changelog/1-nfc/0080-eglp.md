# IGRAPH_EGLP, IGRAPH_GLP_, IGRAPH_FAILURE

**Category**: Core infrastructure

The `IGRAPH_EGLP` error code and all other GLP-specific error codes (starting with `IGRAPH_GLP_`) were removed; functions that used this error code now return `IGRAPH_FAILURE` instead, providing more details in the message associated to the error code.

---

**Original changelog wording:**

> The `IGRAPH_EGLP` error code and all other GLP-specific error codes (starting with `IGRAPH_GLP_`) were removed; functions that used this error code now return `IGRAPH_FAILURE` instead, providing more details in the message associated to the error code.

---

## Proof of work: `git diff --numstat HEAD..next`

Before and after the change, side by side (add-before, del-before, add-after, del-after, file):

```
add-b  del-b  add-a  del-a  file
  11     0     0     0  changelog/1-nfc/0080-eglp.md
  17    34     8    16  include/igraph_error.h
  24    17    23    16  src/core/error.c
  11    13    11    13  src/internal/glpk_support.c
   3     3     0     0  src/internal/glpk_support.h
   4     5     3     4  tests/unit/glpk_error.c
```

Notes on remaining differences:

- `include/igraph_error.h`/`src/core/error.c` retain deltas for other unrelated removed error codes (EDIVZERO, ELAPACK, EDRL, CPUTIME, EATTRIBUTES) handled in later entries.
- `src/internal/glpk_support.c` retains `11/13` for unrelated hunks.
- `tests/unit/glpk_error.c` retains `3/4` for other unrelated test changes.
