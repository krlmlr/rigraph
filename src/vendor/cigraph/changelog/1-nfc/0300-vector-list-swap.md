# igraph_vector_list_swap(), igraph_graph_list_swap()

**Category**: Core data structures

`igraph_vector_list_swap()` and `igraph_graph_list_swap()` no longer return an error code.

---

**Original changelog wording:**

> `igraph_vector_list_swap()` and `igraph_graph_list_swap()` no longer return an error code.
