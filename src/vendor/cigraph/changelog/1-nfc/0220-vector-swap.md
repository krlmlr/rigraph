# igraph_vector_swap(), igraph_matrix_swap()

**Category**: Core data structures

`igraph_vector_swap()` and `igraph_matrix_swap()` no longer return an error code.

---

**Original changelog wording:**

> `igraph_vector_swap()` and `igraph_matrix_swap()` no longer return an error code.
